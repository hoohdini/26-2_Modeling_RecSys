# 발표 준비 가이드

> **발표 준비는 이 문서 하나로 시작하세요.** 각 주장이 어느 문서 어느 절에 있는지, 무엇이 폐기됐는지 적었습니다.
> 전 문서 분류표는 [`README.md`](README.md)(docs/). 최종 갱신 2026-09-19 (9/15~9/19 시드 확장·v4·Temporal 결과 반영).

---

## 1. 30초 스토리

```
문제   생성형 추천(TIGER)은 신규·비인기 아이템을 못 찾는다
가설   아이템 주소(Semantic ID)를 텍스트 대신 그래프로 만들면 나아진다
검증   ① Amazon Beauty   ② 구인구직(자체 구축)  두 도메인
```

**결과 다섯 줄** (9/19 · 원문 `results/reports/`)

1. **Beauty — 확정, 이제 5시드.** G-SID 가 텍스트 주소를 이긴다. NDCG@10 +0.0044(바닥 1.6배), COLD@50 +48%, 5/5 부호 일치.
   Temporal W4·W5 도 30k 레시피로 3시드 재실행해 **세 트랙 3시드 재현**(규칙 C 통과). 승자 β₀ = 1.0.
   튜닝식 vs 고전 블렌딩은 5시드로도 바닥 아래 → **동등하다고 쓴다.** 다양성은 측정 불가 유지.
2. **구인구직 v3 — 주지표는 6시드로도 미확인.** 시드 99 에서 부호가 뒤집혀 짝지은 t 1.34(기준 2.015).
   recall@50·ndcg@50 은 유의. G1 7/7 통과. **"재현됨"은 recall@50·ndcg@50 한정으로만 말한다.**
3. **구인구직 v4 — 재현.** 캐글 실측 분포로 상호작용을 다시 만든 데이터에서 6시드 짝지은 t 가 정확도 4지표 전부 유의
   (NDCG@10 t 11.4), G1 통과. 사전등록 바닥 방식은 n=6 에서 바닥이 커져 recall@50 만 통과 — **두 방식을 나란히 싣는다.**
   v3 와 v4 는 데이터가 달라 수치를 비교하지 않는다.
4. **zero-shot — 못 뚫었다. 대신 원인을 특정했다.** 디코더가 아니라 **SID 할당 커버리지** 문제.
   같은 정보로 콘텐츠 추천기는 23% 를 잡는데 TIGER 는 0.04% — 생성형 검색이 정보를 버린다.
5. **P4·P5 (9/19 저녁).** 그래프 이득은 학습 예산과 무관하다 — 15k·45k 에서도 NDCG@10 2/2 부호 일치(규칙 D 통과).
   Beauty 접두사 제약 덤프는 무효 SID 를 0 으로 만들지만 정확도 −0.1%, COLD +3~4% 에 그치고 zero-shot 은 0 그대로다.
   구인구직 §8 과 같은 결론: **접두사 제약은 zero-shot 을 못 뚫고, 그래프 우위는 제약과 무관하게 유지된다.**

---

## 2. 슬라이드 뼈대와 근거 문서

| 슬라이드 | 핵심 메시지 | 근거 |
|---|---|---|
| 문제 정의 | 콜드 아이템이 추천되지 않는다 | `README.md` · `SHARE_GSID_결과요약.md` §9 |
| 방법 — G-SID | 그래프를 주소에 직교 주입, β₀ 로 회전각 조절 | `HANDOFF_graph_sid.md` · `RESULTS_실험표.md` |
| **결과 ① Beauty** | **N@10 +0.0043 · COLD +47.6% · 3시드 · 세 트랙** | **`SHARE_GSID_결과요약.md`** §3~5 |
| 이득의 위치 | mid +35.2% / low +75.7% / head +3.6% | `SHARE_GSID_결과요약.md` §4 |
| 실패한 대조 2건 | CRAB · 확산 다이얼 둘 다 실패 | `SHARE_GSID_결과요약.md` §8 |
| 도메인 이전 | user=담당자 / item=후보자 / cold=신규 가입자 | `PREREG_생성데이터셋.md` |
| 데이터셋 구축 | 고용24 공공데이터 + 시뮬레이터 | `data_gen/README.md` · `JOBS_데이터셋_구축기록.md` |
| **결과 ② 구인구직** | **v3 6시드: recall@10·50, ndcg@50 유의, 주지표 미확인 · v4 6시드: 4지표 유의, G1 7/7** | **`JOBS_결과정리.md`** §10~11 |
| **zero-shot 분석** | **원인 = SID 커버리지. 세 해법과 대가** | **`JOBS_결과정리.md`** §8 |
| 방법론 기여 | G-L 게이트 · 프로토콜 고정 · GRID 버그 | `JOBS_학습실패_원인분석.md` · `patches/README.md` |
| 한계 | v3 주 지표 미확인 · 다양성 측정 실패 · 순환 잔존 · v3/v4 비교 불가 | `JOBS_결과정리.md` §6 · §11-2 |

---

## 3. 문서 지도

전 문서의 분류(정본 / 배경 / 운영 / 기록)는 [`docs/README.md`](README.md) 에 있다. 발표에 직접 쓰는 것만 다시 적으면:

| 문서 | 내용 |
|---|---|
| **`SHARE_GSID_결과요약.md`** | Beauty 결과 전부. β₀ 스윕 승자, 3시드 검증, 실패한 대조 2건, 한계 (8/31 기준 — 5시드·Temporal 3시드 갱신분은 `results/reports/`) |
| **`JOBS_결과정리.md`** | 구인구직 정본. §10 v3 6시드 판정, §11 v4 재현, §8 zero-shot |
| `DATASET_v4_파이프라인.md` · `results/reports/V4_보고.txt` | v4 파이프라인 · 판정표 원문 |
| **`data_gen/README.md`** | 데이터셋 출처·스키마·재현경로·게이트. 원데이터 vs 생성 경계 |
| `JOBS_학습실패_원인분석.md` · `patches/README.md` | G-L 게이트의 근거 · GRID 접두사 제약 버그 |

### 🔴 주의 — 낡았거나 폐기된 수치

| 문서 | 문제 |
|---|---|
| `_archive/JOBS_야간작업_현황.md` | **v1(폐기본) 시점 기록.** 역사 보존용. 수치 인용 금지 |
| `_archive/TRACK_C_아티팩트.html` | 구 split 기준 |
| `SHARE_GSID_결과요약.md` §1 의 "3시드" 수치 | 5시드 값으로 갱신됨 (`results/reports/GSID_WEEK_보고.txt`). 방향은 같고 크기만 다르다 |

---

## 4. 인용하면 안 되는 수치 (실제로 틀렸던 것들)

발표 준비 중 옛 대화나 문서에서 이 숫자들을 보면 **쓰지 마세요.**

| 잘못된 수치 | 실제 | 왜 틀렸나 |
|---|---|---|
| 구인구직 COLD@50 "3배 개선" | 시드 부호 반전 — **측정 불가** | v2 2시드 기준. 시드 13 에서 음수 |
| 구인구직 다양성 개선 | **측정 불가** | coverage sd 가 text 0.069 / gsid 0.145 |
| 접두사 제약 롱테일 "+16~19%" | **+8%** | 1시드 기준이었음. 3시드로는 절반 |
| v1·v2 의 모든 지표 | **폐기** | 데이터 버그 / 프로토콜 버그 |

**규칙: 1시드 수치는 인용하지 않습니다.** 이 프로젝트에서 세 번 데였습니다.

---

## 5. 예상 질문과 답

**Q. 주 지표(NDCG@10)가 실패했는데 결과라고 할 수 있나?**
방향은 +9.2% 로 맞고 3시드 전부 양수입니다. 유의하지 않은 것은 **효과가 없어서가 아니라
n=3 의 검정력 부족**입니다. 계산해서 보였습니다 — `JOBS_결과정리.md` §4-3.
n=5~6 이면 recall@10 은 거의 확실히, ndcg@10 은 경계선에 올라옵니다.

**Q. 짝지은 검정으로 바꾼 건 결과 보고 기준을 바꾼 것 아닌가?**
맞습니다. 순서가 뒤였다는 사실을 문서에 명시했습니다(§4-2). 근거는 효과가 **같은 시드 안의
짝지은 차이**인데 사전등록 바닥은 **서로 다른 시드 간 산포**라 시드 주효과가 장벽으로
잘못 실린다는 것입니다. 두 검정 결과를 나란히 싣습니다.

**Q. 생성 데이터인데 신뢰할 수 있나?**
직무 어휘와 직업 분류는 **발명하지 않았습니다** — 고용24 공공데이터입니다.
합성한 것은 프로필 조합·텍스트·상호작용뿐이고, 상호작용도 LLM 이 아니라 **명시적 수식**입니다.
경계는 `data_gen/README.md` §1 에 표로 있습니다.

**Q. 그래프가 정답지를 보는 것 아닌가 (순환)?**
완전히 배제하지 못했습니다. 담당자는 요구 차원 20개만 관측잡음(σ=14)을 통해 보고
그래프는 전체 77차원을 쓰도록 분리했지만 결합은 남습니다.
**G1 은 차수 아티팩트를 배제할 뿐 순환을 재지 못합니다.** 숨기지 않고 한계로 적었습니다.

**Q. zero-shot 을 못 뚫었으면 실패 아닌가?**
원인을 수치로 특정했습니다 — zero-shot 의 97.6% 가 학습 미방문 코드 경로 위에 있습니다.
계층을 줄이는 것은 답이 아님을 GPU 8분으로 확인해 재학습 13시간을 아꼈습니다.
그리고 2계층 접두사 확장으로 **0 → 0.061 을 열었습니다**(전체 −15% 대가). §8-7.

---

## 6. 재현하려면

```bash
# 데이터셋
python data_gen/simulate_interactions.py --temp 0.15 --gamma 0.20 --cand-pool 12000
python data_gen/build_splits.py
python data_gen/gate_learnability.py        # G-L. 통과 전 GPU 제출 금지

# 학습·채점 (서버)
bash code/run_jobs_v3.sh                    # 6셀, 약 11시간 30분
bash code/score_jobs_v3.sh
python code/verdict_v3.py                   # 판정표 -> V3_아침보고.txt
```

전체 경로는 `data_gen/README.md` §3.
