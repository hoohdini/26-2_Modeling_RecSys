# 야간 작업 결과 — 2026-08-31

> **08-31 오전 갱신.** 밤새 제출한 작업은 전부 정상 종료했습니다.
> 그런데 **결과를 쓸 수 없습니다.** 데이터셋에 학습할 신호가 없었습니다.
> 원인·근거·복구 계획: [`JOBS_학습실패_원인분석.md`](JOBS_학습실패_원인분석.md)

## 실행 결과

| 작업 | 상태 |
|---|---|
| TIGER 학습 5셀 (151486·487·488·489·579) | ✅ COMPLETED |
| eval dump 5건 (151575~580) | ✅ COMPLETED |
| 자동 채점 | 새벽 1:30 접속 끊김으로 미실행 → 오전 수동 실행, 5건 완료 |
| 151734 FAILED | 무해 — `jobs_rewired_s42` 중복 제출 방어 (이미 151579 로 완료) |

새벽 1:30 의 접속 끊김은 캠퍼스망 쪽 문제였고 Slurm 작업에는 영향이 없었습니다.
로컬 PC 가 재시작된 것도 원격 클러스터 작업과 무관합니다.

## 채점 결과 (`~/recsys/results_jobs/*.json`)

| 셀 | ndcg@10 | recall@10 | coverage@10 |
|---|---|---|---|
| jobs_text_s42 | 0.001290 | 0.00272 | 0.00092 |
| jobs_gsid_b10_s42 | 0.001072 | 0.00224 | 0.00100 |
| jobs_text_s7 | 0.000785 | 0.00176 | 0.00100 |
| jobs_gsid_b10_s7 | 0.000948 | 0.00187 | 0.00108 |
| jobs_rewired_s42 | 0.000914 | 0.00208 | 0.00125 |

## 판정

**H1 기각.** `gsid − text` 가 시드 42 에서 −0.000218, 시드 7 에서 +0.000163 으로
**부호가 뒤집힙니다.** 같은 레시피의 시드 간격(노이즈 바닥)은 text 0.000505 로,
효과보다 큽니다. 사전등록 기준을 그대로 적용하면 기각입니다.

**G1 판정 불가.** 지울 이득이 애초에 없습니다.

**그러나 위 두 판정은 의미가 없습니다.** 모델이 붕괴했기 때문입니다 —
18,738 명 전원에게 사실상 같은 11 개 아이템을 추천했습니다
(`coverage@10 = 0.00092 × 12,000 ≈ 11`, `exposure_gini@10 = 0.9992`).
`MostPopular` 가 recall@10 0.00240 인데 TIGER 가 0.00272 입니다. **1.13 배.**

원인은 모델도 학습 설정도 아니고 **데이터**입니다. 자세한 것은 원인분석 문서로.

## 살아있는 것 / 버리는 것

| | |
|---|---|
| 재사용 | 고용24 수집 1,970회 · 프로필 12,000 · 스킬 벡터 · 영문 텍스트 · 그래프 220,918간선 · **G0 통과** · **SID 3종** |
| 폐기 | 상호작용 로그 · Split A/B · grid_data · TIGER 5셀 |

SID 는 텍스트·그래프 임베딩에서 나오고 상호작용을 보지 않으므로 그대로 씁니다.
재생성이 필요한 것은 `simulate_interactions.py` 아래 단계뿐입니다.

Beauty 본 실험(실험표 ⑤, β₀ 스윕, 커밋된 결과 전부)은 영향 없습니다.

## 새로 추가한 게이트

`data_gen/gate_learnability.py` — **GPU 제출 전 CPU 로 "배울 것이 있는가"를 판정합니다.**
G0 은 그래프 품질 검사지 상호작용 품질 검사가 아니라서 이 실패를 못 잡았습니다.

## 다음 — v2 재학습 제출됨 (08-31 11:14)

시뮬레이터를 재캘리브레이션해 G-L 을 통과시킨 뒤 5셀을 다시 제출했습니다.
**`OBS_NOISE` 는 건드리지 않았습니다** — 순환 위험이 사전등록 설계 그대로입니다.

```
TEMP 16 → 0.15   GAMMA 0.45 → 0.20   CAND_POOL 3000 → 12000
G-L: ContentKNN/Pop 3.20x · ItemKNN/Pop 2.90x  (Beauty 3.52x)  ✅
```

```
153430 jobs_text_v2_s42       → 153431 dump
153432 jobs_gsid_b10_v2_s42   → 153433 dump
153434 jobs_text_v2_s7        → 153435 dump
153436 jobs_gsid_b10_v2_s7    → 153437 dump
153438 jobs_rewired_v2_s42    → 153439 dump
```

아침에 확인할 것:

```bash
squeue -u dsl05
bash ~/recsys/score_jobs_v2.sh        # 여러 번 돌려도 안전
ls ~/recsys/results_jobs_v2/
```

**첫 관문은 `train/loss_step`**. v1 은 9.79 에서 정체했고 Beauty 는 같은 설정에서
5.75 까지 내려갔습니다. 이게 안 내려가면 채점을 볼 필요도 없습니다.

⚠️ 알려진 편차: 노출 쏠림이 상위 50% → 92.6% 입니다 (Beauty 82.4%).
가용한 레버로는 내려가지 않습니다. 근거와 판단은 원인분석 §6 에 있습니다.
