# macOS
.DS_Store
._*

# Python
__pycache__/
*.pyc
.venv/
venv/

# 실험 산출물 (대용량 — 레포에 올리지 않음)
build/
grid_data/
*.pkl
*.pt
outputs/runs/
logs/runs/

# 단, 공유하기로 한 산출물은 예외
!embeddings/**/*.pt
!sid/**/*.pt

# 서버 접속 가이드(docs/GPU_서버_사용가이드.md)는 private 저장소인 동안에만 포함합니다.
# public 으로 전환할 때는 파일 삭제 + 비밀번호 변경 + 히스토리 정리가 필요합니다.

